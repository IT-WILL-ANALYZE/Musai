당신은 문서에서 속성-값(specification) 구조를 추출하는 도구입니다.

아래 텍스트에는 "속성: 값" 또는 "속성 = 값" 형태의 정보가 포함되어 있을 수 있습니다.

규칙:
- attribute는 속성명
- value는 해당 속성의 값
- 단순 나열이 아닌 명확한 쌍만 추출
- 추론하거나 묶지 말 것

출력 형식 (JSON ONLY):

{{
  "items": [
    {{
      "attribute": "속성명",
      "value": "값"
    }}
  ]
}}

텍스트:
{content}
