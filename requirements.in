# =========================================================
# Dependency management is managed using pip-tools.
# 1. install pip-tools
# (venv) pip install pip-tools>=7.5.2
#
# 2. update requirements.txt
# (venv) pip-compile requirements.in -o requirements.txt
#
# 3. install requirements.txt
# (venv) pip install -r requirements.txt
# =========================================================

# Core Framework
streamlit>=1.28.0

# LangChain Core
langchain-core>=0.1.0
langchain-community>=0.0.20
langchain-openai>=0.0.5
langchain-chroma>=0.1.0
langchain-text-splitters>=0.0.1

# Environment Variables
python-dotenv>=1.0.0

# Document Processing
unstructured>=0.10.30

# Vector Database
chromadb>=0.4.0

# Reranking (FlashrankRerank) - not used
# flashrank>=0.2.0

# OpenAI (for embeddings and LLM)
openai>=1.0.0

# Additional dependencies for unstructured
pypdf>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
pandas>=2.0.0

# PDF Processing (PyMuPDF)
pymupdf>=1.23.0
pymupdf4llm==0.0.17

#log
loguru>=0.7.3